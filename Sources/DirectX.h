#ifndef _DIRECTX_H_
#define _DIRECTX_H_

#include "main.h"
/*
class DirectX
{
public:
	bool Init(HINSTANCE hInstance, HWND& hWnd, BOOL bWindow);
	void Uninit();
	void Update();
	void Draw();

	void ImGuiUpdate(void);
	LPDIRECT3DDEVICE9 GetD3DDevice(void) { return g_pD3DDevice; } // Direct3DDeviceの取得

	static DirectX* Inst() { 
		if (instance == nullptr) { instance = new DirectX; }
		return instance; }
private:
	static DirectX* instance;

	// グロバール変数
	LPDIRECT3D9 g_pD3D = NULL;				// DIRECT3D インタフェース
	LPDIRECT3DDEVICE9 g_pD3DDevice = NULL;	// DIRECT3D デバイス
	bool bGui = false;

};

*/

#endif //_DIRECTX_H_

